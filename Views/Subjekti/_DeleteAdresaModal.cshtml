
<div class="modal fade" id="deleteAdresaModal" tabindex="-1"
     aria-labelledby="deleteAdresaModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">

            <form asp-action="ObrisiAdresu" method="post">
                @Html.AntiForgeryToken()

                <input type="hidden" name="id" id="deleteAdresaId" />
                <input type="hidden" name="povratniSubjektId" id="deleteAdresaPovratniSubjektId" />

                <div class="modal-header bg-danger text-white">
                    <h5 class="modal-title" id="deleteAdresaModalLabel">
                        Brisanje adrese
                    </h5>

                    <button type="button" class="btn-close btn-close-white"
                            data-bs-dismiss="modal" aria-label="Zatvori"></button>
                </div>

                <div class="modal-body">
                    <p class="mb-0">
                        Jeste li sigurni da Å¾elite obrisati adresu
                        <strong id="deleteAdresaNaziv"></strong>?
                    </p>
                </div>

                <div class="modal-footer">
                    <button type="submit" class="btn btn-danger">
                        ObriÅ¡i
                    </button>

                    <button type="button" class="btn btn-outline-secondary"
                            data-bs-dismiss="modal">
                        Odustani
                    </button>
                </div>
            </form>

        </div>
    </div>
</div>

<script>
    (function () {
        const modal = document.getElementById('deleteAdresaModal');

        modal.addEventListener('show.bs.modal', function (dogadaj) {
            const gumb = dogadaj.relatedTarget;

            document.getElementById('deleteAdresaId').value =
                gumb.getAttribute('data-id');

            document.getElementById('deleteAdresaPovratniSubjektId').value =
                gumb.getAttribute('data-povratnisubjektid') ?? '';

            document.getElementById('deleteAdresaNaziv').textContent =
                gumb.getAttribute('data-naziv');
        });
    })();
</script>
